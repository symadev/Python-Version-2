data = bytearray([65, 66, 67])  # ASCII → A, B, C
print(data)

#Python represents a bytearray as bytearray(b'...').
#
# b'ABC' is the byte representation of 'ABC'.
#
# This shows the internal bytes in a readable format.